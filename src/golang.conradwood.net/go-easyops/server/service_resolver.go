package server

import (
	"context"
	"fmt"
)

func get_service_id(ctx context.Context, uid string) error {
	fmt.Printf("[go-easyops] getting service by userid \"%s\"\n", uid)
	panic("obsolete codepath")
}
